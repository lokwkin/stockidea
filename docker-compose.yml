version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=stockidea
      - POSTGRES_USER=stockidea_user
      - POSTGRES_PASSWORD=stockidea_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - stockidea-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stockidea_user && psql -U stockidea_user -d stockidea -c 'SELECT 1'"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./output:/app/output
      - ./.cache:/app/.cache
      - ./data:/app/data
    environment:
      - FMP_API_KEY=${FMP_API_KEY}
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_DB=stockidea
      - POSTGRES_USER=stockidea_user
      - POSTGRES_PASSWORD=stockidea_password
    env_file:
      - .env
    command: uv run uvicorn stockidea.api:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - stockidea-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0
    environment:
      - VITE_API_URL=http://backend:8000
    depends_on:
      - backend
    networks:
      - stockidea-network

volumes:
  postgres_data:

networks:
  stockidea-network:
    driver: bridge
